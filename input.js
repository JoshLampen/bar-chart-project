let chartElements = {}
let chartOptions = {}

function drawBarChart(data) {
  for (number of data) {
    let element = 'element' + number.toString();
    return element;
  }
};

drawBarChart([1, 2, 3]);
